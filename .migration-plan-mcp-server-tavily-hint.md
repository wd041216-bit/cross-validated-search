# Migration Plan: Expose Tavily availability in MCP server tool description

**Unit ID:** mcp-server-tavily-hint
**Search Provider:** duckduckgo (ddgs)
**Migration Strategy:** additive

## Objective

Update the MCP server tool description to reflect that Tavily can be enabled via environment variable, so MCP clients can surface this capability to users.

## Implementation Steps

1. Optionally detect TAVILY_API_KEY at server startup and log whether Tavily is active
2. Update the search_web Tool description string to note that Tavily is used alongside DuckDuckGo when TAVILY_API_KEY is configured

## Files to Modify

### `free_web_search/mcp_server.py`
- Update search_web tool description to mention Tavily fallback/parallel option when TAVILY_API_KEY is set; no logic changes needed as UltimateSearcher handles provider selection

## Environment Variable Changes

- TAVILY_API_KEY (already introduced in core-searcher-tavily unit) — MCP server reads it for logging/description purposes only

## Notes

Minimal change; the actual provider routing logic lives in search_web.py. This unit can be skipped if description purity is preferred.
